import * as React from 'react';

import { Container } from './styles';

const GalerieTabbarScreenContainer: React.FC<{}> = ({ children }) => {
    return <Container>{children}</Container>;
};

export default GalerieTabbarScreenContainer;
