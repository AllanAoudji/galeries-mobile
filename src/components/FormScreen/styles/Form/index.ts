import styled from 'styled-components/native';

const Form = styled.Pressable`
    bottom: 0;
    justify-content: flex-end;
    left: 0;
    position: absolute;
    right: 0;
    top: 0;
`;

export default Form;
