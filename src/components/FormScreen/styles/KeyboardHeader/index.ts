import Animated from 'react-native-reanimated';
import styled from 'styled-components/native';

const KeyboardHeader = styled(Animated.View)`
    align-items: center;
    flex-direction: row;
    overflow: hidden;
`;

export default KeyboardHeader;
