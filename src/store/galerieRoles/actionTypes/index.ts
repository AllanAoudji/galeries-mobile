import { BY_ID, GALERIE_ROLES, RESET, SET } from '#store/genericActionTypes';

export const GALERIE_ROLES_BY_ID_RESET = `${GALERIE_ROLES}${BY_ID} ${RESET}`;
export const GALERIES_ROLES_BY_ID_SET = `${GALERIE_ROLES}${BY_ID} ${SET}`;
