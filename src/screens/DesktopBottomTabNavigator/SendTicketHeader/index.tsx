import * as React from 'react';

import { DefaultHeader } from '#components';

const SendTicketHeader = () => {
    return (
        <DefaultHeader
            color="primary-dark"
            textColor="secondary-light"
            title="send ticket"
            variant="secondary"
        />
    );
};

export default SendTicketHeader;
