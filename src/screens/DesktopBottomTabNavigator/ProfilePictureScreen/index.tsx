import * as React from 'react';
import { Typography } from '#components';

const ProfilePictureScreen = () => {
    return <Typography>profile picture screen</Typography>;
};

export default ProfilePictureScreen;
