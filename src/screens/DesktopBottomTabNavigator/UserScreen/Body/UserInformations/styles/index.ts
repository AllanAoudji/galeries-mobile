import styled from 'styled-components/native';

const Container = styled.View`
    align-items: center;
`;
const UserPseudonymContainer = styled.View`
    align-items: flex-start;
    flex-direction: row;
`;

export { Container, UserPseudonymContainer };
