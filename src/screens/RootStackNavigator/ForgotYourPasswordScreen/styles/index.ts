import styled from 'styled-components/native';

const TextContainer = styled.View`
    margin: ${({ theme }) => `6px 0 ${theme.spacings.small} 0`};
`;

// eslint-disable-next-line import/prefer-default-export
export { TextContainer };
