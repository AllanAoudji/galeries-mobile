import * as React from 'react';
import { Typography } from '#components';

const DeleteAccountScreen = () => {
    return <Typography>Delete account</Typography>;
};

export default DeleteAccountScreen;
