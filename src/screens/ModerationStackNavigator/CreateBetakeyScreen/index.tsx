import * as React from 'react';

import { Typography } from '#components';

const CreateBetakeyScreen = () => {
    return <Typography>create beta key screen</Typography>;
};

export default CreateBetakeyScreen;
